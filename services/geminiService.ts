// AI features have been removed from this application.
export const draftEmailReply = async (originalEmailContent: string, tone: string = "professional"): Promise<string> => {
  return "AI features are disabled.";
};